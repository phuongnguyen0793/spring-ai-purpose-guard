# Spring AI BOM version — kept here because io.spring.dependency-management
# resolves it via project.property() before the Gradle version catalog
# type-safe accessors are available inside its dependencyManagement {} block.
springAiVersion=1.0.0
